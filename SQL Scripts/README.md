# SQL Scripts

This folder contains SQL Server scripts for schema creation, staging tables, reference tables, exception tables, audit tables, warehouse tables, validation logic, and final ETL review queries.
